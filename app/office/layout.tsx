import OfficeLayout from "@/components/office/shared/OfficeLayout";

export default function Layout({ children }: { children: React.ReactNode }) {
    return <OfficeLayout>{children}</OfficeLayout>;
}
